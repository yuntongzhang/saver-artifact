--- /home/saver/effFix-benchmark/swoole/src/src/core/socket.c	2023-07-21 19:03:41.000000000 +0900
--- /home/saver/effFix-benchmark/swoole/src/src/core/socket.c	2023-07-21 19:03:41.000000000 +0900
@@ -32,7 +32,11 @@
     struct stat file_stat;
     if (fstat(file_fd, &file_stat) < 0)
     {
-        swWarn("fstat() failed. Error: %s[%d]", strerror(errno), errno);
+        swWarn("fstat() {
+    close(file_fd);
+failed. Error: %s[%d]", strerror(errno), errno);
+}
+
         return SW_ERR;
     }
 
