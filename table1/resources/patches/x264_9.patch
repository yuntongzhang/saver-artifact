===== Patch instructions generated by SAVER =====
[+] { Insert: if (true) free(*(open_file:h)) at 14 (line 60, column 10) }
===== Actual patch in diff format =====
--- a/table1/benchmarks/x264/input/thread.c
+++ b/table1/benchmarks/x264/input/thread.c
@@ -58,6 +58,7 @@ static int open_file( char *psz_filename, hnd_t *p_handle, video_info_t *info, c
     h->next_frame = -1;
     h->next_args = malloc( sizeof(thread_input_arg_t) );
     if( !h->next_args )
+        free(h);
         return -1;
     h->next_args->h = h;
     h->next_args->status = 0;
