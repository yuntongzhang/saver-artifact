===== Patch instructions generated by SAVER =====
[+] { Insert: if (true) free(*(init:h)) at 35 (line 80, column 9) }
===== Actual patch in diff format =====
--- a/table1/benchmarks/x264/filters/video/crop.c
+++ b/table1/benchmarks/x264/filters/video/crop.c
@@ -78,6 +78,7 @@ static int init( hnd_t *handle, cli_vid_filter_t *filter, video_info_t *info, x2
     int err = handle_opts( h, info, opts, optlist );
     free( opts );
     if( err )
+        free(h);
         return -1;
 
     h->dims[2] = info->width  - h->dims[0] - h->dims[2];
