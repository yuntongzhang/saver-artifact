===== Patch instructions generated by SAVER =====
[+] { Insert: if (true) free(*(open_file:h)) at 64 (line 360, column 9),
===== Actual patch in diff format =====
--- a/table1/benchmarks/x264/input/timecode.c
+++ b/table1/benchmarks/x264/input/timecode.c
@@ -357,8 +357,10 @@ static int open_file( char *psz_filename, hnd_t *p_handle, video_info_t *info, c
             h->timebase_num = strtoul( opt->timebase, NULL, 10 );
             h->timebase_den = 0; /* set later by auto timebase generation */
         }
-        FAIL_IF_ERROR( h->timebase_num > UINT32_MAX || h->timebase_den > UINT32_MAX,
-                       "timebase you specified exceeds H.264 maximum\n" );
+        if ( h->timebase_num > UINT32_MAX || h->timebase_den > UINT32_MAX )
+            x264_cli_log( "timecode", X264_LOG_ERROR, "timebase you specified exceeds H.264 maximum\n" );
+            free(h);
+            return -1;
     }
     h->auto_timebase_num = !ret;
     h->auto_timebase_den = ret < 2;
