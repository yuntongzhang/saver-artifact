===== Patch instructions generated by SAVER =====
[+] { Insert: if (true) free(*(init:h)) at 9 (line 47, column 9) }
===== Actual patch in diff format =====
--- a/table1/benchmarks/x264/filters/video/source.c
+++ b/table1/benchmarks/x264/filters/video/source.c
@@ -45,6 +45,7 @@ static int init( hnd_t *handle, cli_vid_filter_t *filter, video_info_t *info, x2
     h->cur_frame = -1;
 
     if( cli_input.picture_alloc( &h->pic, *handle, info->csp, info->width, info->height ) )
+        free(h);
         return -1;
 
     h->hin = *handle;
